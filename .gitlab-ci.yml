# Combined GitLab CI reference (project moved to GitHub)
# Left here for reference — not active.

#variables:
#  GRADLE_OPTS: "-Dorg.gradle.daemon=false"
#
#stages:
#  - build
#  - test
#  - sonar
#  - deploy
#
#default:
#  cache:
#    key: ${CI_COMMIT_REF_SLUG}
#
# --- Backend ---
#
#backend-build:
#  stage: build
#  script:
#    - cd backend && ./gradlew assemble
#  artifacts:
#    paths:
#      - backend/build/classes
#      - backend/build/libs
#    expire_in: 1h
#
#backend-test:
#  stage: test
#  script:
#    - cd backend && ./gradlew test
#  artifacts:
#    paths:
#      - backend/build/reports/jacoco/test/jacocoTestReport.xml
#
#backend-sonar:
#  stage: sonar
#  script:
#    - cd backend && ./gradlew sonar "-Dsonar.projectKey=solen_be" "-Dsonar.host.url=http://localhost:9000" "-Dsonar.token=$SONAR_TOKEN_MAC"
#  allow_failure: true
#  dependencies:
#    - backend-build
#    - backend-test
#
#backend-docker:
#  stage: deploy
#  script:
#    - docker build -t solen_be:latest ./backend
#    - docker tag solen_be:latest $AWS_ECR_URI:latest
#    - docker push $AWS_ECR_URI:latest
#  only:
#    - main
#
# --- Frontend ---
#
#frontend-install:
#  stage: build
#  script:
#    - cd frontend-web && npm install
#  artifacts:
#    paths:
#      - frontend-web/node_modules/
#    expire_in: 1h
#
#frontend-build:
#  stage: build
#  script:
#    - cd frontend-web && npm run build
#  artifacts:
#    paths:
#      - frontend-web/dist/
#
#frontend-test:
#  stage: test
#  script:
#    - cd frontend-web && npm run test -- --run --coverage
#  artifacts:
#    when: always
#    paths:
#      - frontend-web/coverage/
#
#frontend-deploy:
#  stage: deploy
#  only:
#    - main
#  script:
#    - aws s3 sync frontend-web/dist/ s3://$S3_BUCKET --delete
